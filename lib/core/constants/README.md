# Core Constants

Game-wide constants and configuration values.

## Purpose
- Define game constants (dice faces, roll limits, bonus thresholds)
- Centralize configuration for easy maintenance

## Files
- `dice_faces.dart` - Card-style dice face values (9, 10, J, Q, K, A)
- `game_constants.dart` - Game rules constants (max rolls, bonus threshold)